# Intentionally empty. Mounted over server.conf's `include ssl-listen.conf`
# in the container path (compose.yaml `nginx` service) so it serves plain
# HTTP only. The native path uses ssl-listen.conf (with the 8443 SSL/QUIC
# listeners) instead.
